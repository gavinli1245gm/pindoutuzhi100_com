---
import { siteConfig } from '@/config';

const { site, nav } = siteConfig;
---

<header class="sticky top-0 z-50 cute-gradient backdrop-blur-md border-b border-pink-200/50 dark:border-purple-300/30">
  <div class="max-w-6xl mx-auto px-4 h-16 flex items-center justify-between">
    <a href="/" class="text-xl font-bold text-gray-800 dark:text-gray-100 hover:text-primary-dark transition-colors" style="font-family: var(--font-display)">
      {site.logo ? <img src={site.logo} alt={site.name} class="h-8 dark:filter-[invert(1)_brightness(2)_grayscale(100%)]" /> : site.name}
    </a>

    <nav class="hidden md:flex items-center gap-6">
      {nav.map((item) => (
        <a
          href={item.href}
          class="text-sm text-gray-700 dark:text-gray-200 hover:text-primary-dark transition-colors px-4 py-2 rounded-full hover:bg-white/50 dark:hover:bg-white/20"
          target={item.external ? '_blank' : undefined}
          rel={item.external ? 'noopener noreferrer' : undefined}
        >
          {item.label}
        </a>
      ))}
    </nav>

    <button
      id="mobile-menu-btn"
      class="md:hidden p-2 text-gray-700 dark:text-gray-200 hover:text-primary-dark transition-colors"
      aria-label="菜单"
    >
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
      </svg>
    </button>
  </div>

  <div id="mobile-menu" class="hidden md:hidden border-t border-pink-200/50 dark:border-purple-300/30 bg-white/90 dark:bg-slate-900/90 backdrop-blur-md">
    <div class="px-4 py-3 space-y-2">
      {nav.map((item) => (
        <a
          href={item.href}
          class="block py-2 text-sm text-gray-700 dark:text-gray-200 hover:text-primary-dark px-4 rounded-full hover:bg-pink-100/50 dark:hover:bg-purple-900/30 transition-colors"
          target={item.external ? '_blank' : undefined}
        >
          {item.label}
        </a>
      ))}
    </div>
  </div>
</header>

<script>
  const btn = document.getElementById('mobile-menu-btn');
  const menu = document.getElementById('mobile-menu');
  btn?.addEventListener('click', () => menu?.classList.toggle('hidden'));
</script>
